import { ChunkSizePipe } from './chunk-size.pipe';

describe('ChunkSizePipe', () => {
  it('create an instance', () => {
    const pipe = new ChunkSizePipe();
    expect(pipe).toBeTruthy();
  });
});
